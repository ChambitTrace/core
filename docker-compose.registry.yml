services:
  registry:
    image: registry:2
    container_name: chambit-registry
    restart: unless-stopped
    ports:
      - "5001:5000"
    volumes:
      - registry_data:/var/lib/registry

volumes:
  registry_data:
